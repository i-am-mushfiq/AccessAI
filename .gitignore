node_modules/
.next/
# Scratch output from `npm run build:verify`, which keeps a verification build
# out of the dev server's .next — see scripts/build-verify.mjs.
.next-verify/
out/
build/
dist/
*.tsbuildinfo
next-env.d.ts

# Local database + generated artefacts
data/
drizzle/meta/_journal.json.bak

# Secrets — never commit (PRD §87)
.env
.env.local
.env.*.local

# Editor / OS
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# Test output
coverage/
.vitest/
